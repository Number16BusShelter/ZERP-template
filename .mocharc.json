{
  "timeout": 60000,
  "extension": ["js", "ts"],
  "ui": "bdd",
  "reporter": "spec",
  "spec": [
    "{services,packages}/**/*.test.ts",
    "{services,packages}/**/*.spec.ts"
  ],
  "exit": true
}
